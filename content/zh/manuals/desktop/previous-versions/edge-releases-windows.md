**已知问题**

* 宿主机文件系统共享仅支持 UTF-8 编码的密码。
* Docker 会自动禁用残留的网络适配器。移除它们的唯一方法是按照 [故障排除](/manuals/desktop/troubleshoot-and-support/troubleshoot/known-issues.md) 中的说明，手动使用 `devmgmt.msc` 操作。

### Beta 22 发布 (2016-08-11 1.12.0-beta22)

未发布。变更请参阅 Beta 23。

**已知问题**

* Docker 会自动禁用残留的网络适配器。移除它们的唯一方法是按照 [故障排除](/manuals/desktop/troubleshoot-and-support/troubleshoot/known-issues.md) 中的说明，手动使用 `devmgmt.msc` 操作。

### Beta 21 发布 (2016-07-28 1.12.0-beta21)

**新特性**

* Windows 版 Docker 现在提供 2 个频道：**Stable（稳定版）** 和 **Beta（测试版）**。新功能和 Bug 修复将首先向 Beta 频道的用户进行自动更新。Stable 频道的更新频率较低，且与 Docker 引擎的大版本和小版本发布同步。只有经过充分测试且生产就绪的功能才会添加到 Stable 频道版本中。欲下载两者及了解更多信息，请参阅 [在 Windows 上安装 Docker Desktop](/manuals/desktop/setup/install/windows-install.md)。

* 移除了 Docker 宿主机名。带有导出端口的容器可以通过 localhost 访问。

* UI 会显示用户当前处于 Beta 还是 Stable 频道。

**升级**

* Docker 1.12.0 及其实验性功能。
* Docker Machine 0.8.0
* Docker Compose 1.8.0

**错误修复与微小改进**

* 修复了通过公开端口向容器传输数据时的网络问题。
* 包含了 `qemu-img` 的源代码。
* 修复了当用户登录名包含空格时从 Toolbox 迁移的问题。
* 当 Hyper-V 尚未激活时，禁用从 Toolbox 迁移的功能。
* 更多的窗口支持使用 ESC 键关闭。
* 在崩溃报告中增加了频道信息。
* 修复了 Windows Insider 内部版本 14367 上发生的路径重写 Bug。
* 简化了 `MobyLinux.ps1` 脚本。

**已知问题**

* 旧版本的 Kitematic 与 Windows 版 Docker 不兼容。在点击系统托盘中的 **Open Kitematic...** 链接之前，您需要手动删除 `C:\Program Files\Docker\Kitematic` 文件夹。

### Beta 20 发布 (2016-07-19 1.12.0-rc4-beta20)

**新特性**

* 移除了 UI 中禁用向 `localhost` 转发端口的选项。

**错误修复与微小改进**

* 修复了 `docker.sock` 权限问题。
* 设置面板打开时不执行更新检查。
* 移除了过时的 DNS 临时补丁。
* 在更多情况下使用备用 DNS 服务器。
* 限制并发端口转发的数量以避免资源耗尽。
* 将数据库存储为“裸 (bare)” Git 仓库以避免损坏问题。

### Beta 19 发布 (2016-07-14 1.12.0-rc4-beta19)

**新特性**

* 增加了选择退出发送使用统计信息的选项（目前仅限 Edge 频道）。
* 新增用于上传崩溃报告的错误对话框。

**升级**

* Docker 1.12.0 RC4
* Docker Compose 1.8.0 RC2
* Docker Machine 0.8.0 RC2
* Linux 内核 4.4.15

**错误修复与微小改进**

* `com.docker.slirp`：包含了 DNS TCP 回退修复，这在 UDP 响应被截断时是必须的。
* `docker build/events/logs/stats...` 在被 Ctrl-C 中断时不再发生泄漏。
* 正在下载版本时，禁用更新窗口中的所有按钮。

### Beta 18.1 发布 (2016-07-07 1.12.0-rc3-beta18.1)

> [!NOTE]
>
> Docker 1.12.0 RC3 版本引入了与 RC2 不兼容的变更。您可以通过重新创建或更新容器来解决此问题。

**热修复**

* 修复了导致“Hijack is incompatible with use of CloseNotifier”错误的问题，撤销了之前对构建期间 `Ctrl-C` 的修复。

**新特性**

* 现在默认将端口转发到 localhost。
* 在设置中增加了 `http`/`https` 代理配置。
* 首次启动时可以导入 Toolbox 的默认机器。
* 增加了收集和上传崩溃报告时的 UI。
* 更新检查每 6 小时运行一次。

**升级**

* Docker 1.12.0 RC3

**错误修复与微小改进**

* Docker API 代理曾无法处理 1.12 的某些功能（例如健康检查）。
* 强制结束虚拟机进程时，忽略进程已停止的情况。
* 停止虚拟机时，始终停止 Docker 代理。
* 防止更新窗口将 `.msi` 下载到 `C:\Program Files\Docker`。
* Docker 启动时应禁用所有设置。（此回归问题是在 Beta 17 中引入的）。
* VPNKit：随着网络连接数量的增加，提高了可扩展性。
* 改进了与数据库的连接。
* 在关机服务不可用时予以忽略。

### Beta 18 发布 (2016-07-06 1.12.0-rc3-beta18)

**新特性**

* 现在默认将端口转发到 localhost。
* 在设置中增加了 `http`/`https` 代理配置。
* 首次启动时可以导入 Toolbox 的默认机器。
* 增加了收集和上传崩溃报告时的 UI。
* 更新检查每 6 小时运行一次。

**升级**

* Docker 1.12.0 RC3

**错误修复与微小改进**

* 使用 Ctrl-C 中断 `docker build` 现在会真正停止构建。
* Docker API 代理曾无法处理 1.12 的某些功能（例如健康检查）。
* 强制结束虚拟机进程时，忽略进程已停止的情况。
* 停止虚拟机时，始终停止 Docker 代理。
* 防止更新窗口将 `.msi` 下载到 `C:\Program Files\Docker`。
* Docker 启动时应禁用所有设置。（此回归问题是在 Beta 17 中引入的）。
* VPNKit：随着网络连接数量的增加，提高了可扩展性。
* 改进了与数据库的连接。
* 在关机服务不可用时予以忽略。

### Beta 17 发布 (2016-06-29 1.12.0-rc2-beta17)

**升级**

* Linux 内核 4.4.14，AUFS 20160627。

**错误修复与微小改进**

* 支持登录名中包含空格的用户。
* 修复了某些情况下 `dotnet restore` 可能挂起的问题。
* 修复了对镜像执行 `docker inspect` 的问题。
* 从 Hyper-V 管理器中移除了控制台。
* 改进了 VPN 连接的诊断，并增加了服务端口开启器的日志。
* 改进了 Moby 的启动序列，以适应运行 Swarm 服务时较长的启动时间。
* 强制关闭无法正常关闭的虚拟机。
* 点击变更日志中的链接将打开浏览器。
* 修复了指向文档的链接。
* 修复了下载 Kitematic 的 URL。
* 更新了签名证书。
* 修复了防火墙和网络交换机的错误。
* 修复了 PowerShell 脚本中的解析错误。

### Beta 16 发布 (2016-06-17 1.12.0-rc2-beta16)

**升级**

* Docker 1.12.0 RC2
* Docker Compose 1.8.0 RC1
* Docker Machine 0.8.0 RC1
* Alpine 3.4

**错误修复与微小改进**

* 对 VPN 模式进行了修复。
* 修复了 localhost 端口转发的性能问题。
* 自动检测共享列表中的驱动器挂载/卸载状态。
    - 将应用程序名称从 "DockerforWindows" 更改为 "Docker for Windows"。
    - 避免同时显示多个更新窗口。

### Beta 15 发布 (2016-06-10 1.11.2-beta15)

**新特性**

* 新增实验性网络模式，在 `localhost` 上公开容器端口。
* 新增用于配置 `sysctl.conf` 的设置菜单。
* 新增用于配置 HTTP 代理的设置菜单。
* 移除了 VPN 模式设置（现在 VPN 模式是唯一受支持的模式）。
* 移除了 vSwitch NAT 配置。

**升级**

* Docker 1.11.2
* Linux 4.4.12，AUFS 20160530。

**错误修复与微小改进**

* 将“从 Toolbox 导入”选项移至常规设置。
* 增加了写入配置数据库的超时时间。
* 修复了向 Mixpanel 发送匿名统计信息导致应用程序停止的问题。
* 缩短了启动时间。
* 所有命名管道现在都以单词 `docker` 为前缀。
* 更新窗口中现在显示完整的版本号。
* 默认的守护进程配置不再启用调试模式。
* 响应更快的设置面板，还有新的鲸鱼图标 :-)
* 改进了日志和调试信息。

### Beta 14 发布 (2016-06-02 1.11.1-beta14)

**新特性**

* 启用了 Docker 守护进程的配置（编辑 `config.json`）。
* 默认启用 VPN 模式。
* 移除了虚拟机网络配置中的 DHCP。
* 用户可配置 NAT 前缀和 DNS 服务器。
* 新增用于上传诊断对话框的反馈窗口。
* **Settings** 窗口中新增状态指示器。
* 虚拟机日志随崩溃报告一同上传。
* 动画版的欢迎鲸鱼。

**错误修复与微小改进**

* 密码支持非 ASCII 字符。
* 修复了取消共享驱动器的操作。
* 修复了从服务发送的异常的反序列化问题。
* 如果后端服务未运行，GUI 现在会启动它。
* 如果后端服务未运行且用户只想关闭应用，应用不再报错。


**已知问题**

* 由于 Windows NAT 实现的限制，需要谨慎管理与其他 NAT 前缀的共存。

### Beta 13 发布 (2016-05-25 1.11.1-beta13)

**新特性**

此 Beta 版本包含一些重大更改：

* Docker 通信通过 Hyper-V 套接字而非网络进行。
* 实验性的 VPN 模式，也称为 `vpnkit`。
* 初步支持使用 `datakit` 进行配置。
* 重新设计的设置面板。
* Docker 现在可以重启。

**错误修复与微小改进**

* 支持名称非 "vEthernet (DockerNAT)" 的网络适配器。
* 共享功能现在对域用户有更好的支持。
* 修复了 Toolbox 迁移（在 Beta 12 中已损坏）。
* 启用 Hyper-V 功能（在 Beta 12 中已损坏）。
* 修复了向 `docker run` 传递无效标签时的错误消息。
* Mixpanel 不再使用漫游应用数据 (roaming App Data)。
* UI 改进。
* 增加了对 `10.0.75.0/24` 范围之外其他 IP 地址虚拟机的支持。
* 改进了 FAQ。

**已知问题**

* 由于 Windows NAT 实现的限制，需要谨慎管理与其他 NAT 前缀的共存。

### Beta 12 发布 (2016-05-17 1.11.1-beta12)

**新特性**

* 应用程序现在分为两部分：后端服务和前端 GUI。前端 GUI 不再要求提升权限。

**错误修复与微小改进**

* 从共享列表中排除了网络驱动器。
* 移除了关闭应用程序时的通知。
* 细微的 GUI 改进。

**已知问题**

* 由于 Windows NAT 实现的限制，需要谨慎管理与其他 NAT 前缀的共存。


### Beta 11b 发布 (2016-05-11 1.11.1-beta11b)

**热修复**

* 修复了导致 Docker 无法启动的命名管道权限问题。

### Beta 11 发布 (2016-05-10 1.11.1-beta11)

**新特性**

* GUI 现在以非提升模式运行，并连接到一个提升权限的 Windows 服务。
* 以 256 MB 为增量分配虚拟机内存，而不是 1 GB。
* 当用户密码为空时显示有意义的错误。
* 改进了 [故障排除](/manuals/desktop/troubleshoot-and-support/troubleshoot/_index.md) 页面。

**升级**

* Docker Compose 1.7.1（参见 [变更日志](https://github.com/docker/compose/releases/tag/1.7.1)）。
* 内核 4.4.9。

**错误修复与微小改进**

* 在 `docker port` 中报告虚拟机的 IP。
* 处理包含空格的密码。
* 在尝试安装在家庭版 (Home) 系统上时显示清晰的错误消息。
* 减慢了系统托盘中的鲸鱼动画。
* 代理在崩溃时会自动重启。
* DHCP 进程能优雅地处理异常。
* Moby (后端) 修复：
  - 修复了 `vsock` 半关闭问题。
  - 增加了 NFS 支持。
  - 主机名现在是 Moby，而不是 Docker。
  - 修复了磁盘格式化脚本。
  - Linux 内核升级至 4.4.9。

**已知问题**

* 由于 Windows NAT 实现的限制，需要谨慎管理与其他 NAT 前缀的共存。详情请参阅 [已知问题](/manuals/desktop/troubleshoot-and-support/troubleshoot/known-issues.md)。

* Windows 服务的日志未与 GUI 的日志聚合。预计将在未来版本中修复。


### Beta 10 发布 (2016-05-03 1.11.0-beta10)

**新特性**

* 改进了设置面板，允许您配置虚拟机的内存和 CPU。
* 支持与多个内部 Hyper-V 交换机共存，并改进了 DHCP 处理。
* 令牌验证现在通过 HTTPS 进行。这应能修复某些防火墙和杀毒软件带来的问题。

**升级**

* Docker 1.11.1。

**错误修复与微小改进**

* 修复了桌面快捷方式名称并更新了图标。
* 为将后端作为服务运行做准备。
* 改进了日志记录和 Mixpanel 事件。
* 优化了代码质量。
* 改进了构建过程。
* 全新的图标。

**已知问题**

* 由于 Windows NAT 实现的限制，需要谨慎管理与其他 NAT 前缀的共存。详情请参阅 [故障排除](/manuals/desktop/troubleshoot-and-support/troubleshoot/_index.md)。


### Beta 9 发布 (2016-04-26 1.11.0-beta9)

**新特性**

* 提供了用于启用 Hyper-V 的一键式对话框。
* 报告清晰的底层 Hyper-V 错误。

**错误修复与微小改进**

* 更好地处理某些网络问题。
* 修复了帮助菜单和开始菜单的入门 URL。
* 恢复了首次运行时的 “Docker is Initializing” 通知。
* 优化了身份验证期间的错误消息。
* 改进了错误条件下的日志记录。
* 改进了构建和测试。

**已知问题**

* 如果存在多个内部 Hyper-V 交换机，Moby 虚拟机可能无法正常启动。我们已确认此问题并正在研究解决方案。

### Beta 8 发布 (2016-04-20 1.11.0-beta8)

**新特性**

* 自动更新会静默安装，并在完成后重新启动应用程序。
* 在 Windows 菜单中可以找到卸载程序。
* 可以从控制面板菜单下载 Kitematic。

**错误修复与微小改进**

* 优化了共享驱动器窗口的 UI。
* 防火墙警报对话框出现的频率不再像以前那样频繁。
* 配置 MobyLinux 虚拟机使用 2GB 固定内存。
* 用户密码不再存储在宿主机端的 KVP 中。
* 注册表中提供了卸载快捷方式。

### Beta 7 发布 (2016-04-12 1.11.0-beta7)

**新特性**

  - 支持共享多个驱动器。
  - 全新的更新窗口。
  - 欢迎鲸鱼。

**升级**

* Docker 1.11.0-rc5
* Docker Machine 0.7.0-rc3
* Docker Compose 1.7.0-rc2

**错误修复与微小改进**

* 改进了网络配置和错误检测：修复了 DHCP 续约和重新绑定问题。
* 允许 DNS/DHCP 进程在绑定错误时重启。
* 降低了从 Docker Toolbox 迁移时的破坏性。
* 完善了文档。
* 更好的错误处理：如果启动时间过长，Moby 会自动重启。
* 在安装新版本之前结束代理进程并退出 Docker。
* 应用程序现在无法启动两次。
* 代理在 GUI 未运行时自动停止。
* 在启动 Moby 之前移除了现有的代理防火墙规则。
* 应用程序现在可以收集更多且更优质的关于崩溃和其他问题的信息。
* 优化了所有对话框和窗口。
* 在安装程序的首屏增加了版本号。
* 更好的恢复默认设置功能。
* 全新的回归测试框架。
* 安装 MSI 软件包现在带有时间戳。
* 只有在存在 Docker Toolbox 时，Hyper-V 安装才会提到它。
* 改进了 Bugsnag 报告：修复了一个依赖项 Bug，并为每份新报告增加了唯一 ID。
* 改进了构建过程。
* 优化了代码质量。

**已知问题**

- 设置现在以 JSON 序列化。此次安装会丢失当前设置。

- Docker 需要在防火墙上打开端口。有时用户会看到防火墙警报对话框。用户应允许打开端口。

- 应用程序已升级至 64 位。安装路径更改为 `C:\Program Files\Docker\Docker`。用户可能需要关闭更新前已打开的所有 PowerShell/Cmd 窗口以获取新的 `PATH`。在某些情况下，用户可能需要注销并重新登录。

**错误修复**

  - 修复了 DHCP 续约和重新绑定。
  - 只有在存在 Toolbox 时，Hyper-V 安装才会提到它。
  - 应用程序现在不会启动两次。
  - 现允许 DNS/DHCP 进程在绑定错误时重启。
  - 移除了 Bugsnag 报告期间快速打开的窗口。
  - 修复了 Bugsnag 报告的操作系统信息。
  - 改进了构建过程。
  - 优化了代码质量。

### Beta 6 发布 (2016-04-05 1.11.0.1288)

**增强功能**

- 文档已针对 Beta 6 更新！
- 支持漫游：DNS 查询会转发到宿主机。
- 通过在宿主机上运行 DHCP 服务器缩短了启动时间。
- 全新的设置对话框设计。
- 支持在 `-v` 中使用 Windows 路径。
* 将 Docker CLI 和守护进程更新至 1.11.0-rc3。
* 将 Docker Machine 更新至 0.7.0-rc2。
* 将 Docker Compose 更新至 1.7.0-rc1。
- 现在会安装 `docker-credential-wincred`。
- 允许容器中的非 root 用户在卷挂载上创建文件。
- 自动安装 Hyper-V。
- 应用程序现在是 64 位。
- 优化了所有对话框和错误消息的措辞。
- 移除了退出确认。
- 在安装 HyperV 的消息框中显示可点击的 URL。
- 控制面板增加了指向 Kitematic 的链接（与 Mac 上一致）。
- Moby 内核更新至 4.4.6。
- 注册表项已更改为 `HKLM\SOFTWARE\Docker Inc.\Docker\1.0`。

**已知问题**

- 从 Docker Toolbox 迁移有时会失败。如果发生这种情况，变通方法是重启应用程序。

- Docker 需要在防火墙上打开端口，这可能会激活防火墙警报对话框。用户应允许打开端口。

- 应用程序已升级至 64 位。安装路径更改为 `C:\Program Files\Docker\Docker`。如果用户在更新前已打开 PowerShell/Cmd 窗口，可能需要关闭它们以捕获新的 `PATH`。在某些情况下，用户需要注销并重新登录。

**错误修复**

- 结束无法正常关闭的虚拟机。

- 改进了随 Bugsnag 报告发送的诊断信息。

- 设置窗口会显示驱动器是否已共享。
`C:` 驱动器可以使用 `//c` 或 `/c` 进行绑定挂载。以前是 `//c/`。

- 不要尝试提交空令牌。

- 修复了“关于”框中显示的版本号。

- 修复了日志中的竞态条件。

- 修复了设置中的竞态条件。

- 修复了文档中损坏的链接。

- 在程序集中用实际版本号替换了 `sha1`。

- 不再启动未使用的代理进程。

### Beta 5 发布 (2016-03-29 1.10.6)

**增强功能**

* 移除了调试控制台。
* 打开浏览器并显示 Hyper-V 安装说明。
* 为欧洲地区的下载增加了 CloudFront 支持。
* 在 Toolbox 升级期间捕获 QEMU 日志。
* 将 Alpha 分发频道重命名为 Beta 频道。

**错误修复**

* 修复了 Bugsnag 报告中的诊断部分。
* 修复了 MSI 版本号。
* 不再截断 Toolbox 链接。

> [!NOTE]
>
> 此时 Windows 版 Docker 从 Beta 1 直接跳到了 Beta 5，以便与稍早进入测试周期的 Mac 版 Docker 版本号保持同步。

### Beta 1 发布 (2016-03-24 1.10.6)

**增强功能**

- 显示第三方许可协议。
- 显示许可协议。
- 如果未启用 Hyper-V，应用程序将拒绝启动。
- 将 `console` 重命名为 `debug console`。
- 从通知中移除 `machine`。
- 打开反馈论坛。
- Windows 和 macOS 使用相同的 MixPanel 项目。
- 使 MixPanel 事件与 macOS 保持一致。
- 增加了一个用于诊断问题的脚本。
- 随 Bugsnag 报告提交诊断信息。
- 每小时执行一次 MixPanel 心跳。

**错误修复**

- 安装期间接受所有版本的 Enterprise 10、Pro 10 和 Education 10 (Eval, N, ...)。
- 修复了某些应用程序导致的 Linux 内核崩溃。
- 修复了不显示的通知。
- 重置时系统托盘的鲸鱼图标会有动画效果。
- 缩短了注册过程的超时时间。
- 当用户取消选择该设置时，正确卸载共享。
- 不在不受支持的版本上安装。

### Alpha 4 发布 (2016-03-10 1.10.4.0)

- 更快的启动和关闭。
- 使用宿主机 DNS 参数。
- 注册系统。
- 重新创建被手动移除的虚拟机。
- 更多的 MixPanel 事件。
- 各种 Bug 修复。

### Alpha 3 发布 (2016-03-03 1.10.2.14)

**文件共享**

  - 自动创建网络共享。
  - 改进了凭据管理。
  - 支持带 c 和 C 驱动器盘符的路径。

**崩溃与分析**

  - 通过 Bugsnag 报告崩溃。
  - 通过 MixPanel 发送分析数据。

**GUI**

  - 优化了“关于”和“设置”对话框的布局。
  - 改进了更新程序。
  - 增加了指向 *Help* 的链接。
  - 增加了指向 *Send Feedback* 的链接。

**常规**

  - Bug 修复。

### Alpha 2 发布 (2016-02-26 1.10.2.12)

**安装程序**

  - 增强功能。
  - 自动更新。
  - 许可协议。

**常规**

  - Bug 修复。

### Alpha 1 发布 (2016-02-22 1.10.1.42-1)

**虚拟机管理程序 (Hypervisor)**

  - 显著的性能提升。

**安全**

  - 从用户处获取凭据。

**文件系统**

  - 使用凭据热挂载宿主机文件系统。

**常规**

  - 状态管理。
  - 稳定性、日志。
  - Bug 修复、界面优化。

### Alpha 0 发布 (2016-02-09 1.10.0.0-0)

**虚拟机管理程序 (Hypervision)**

  - 基于 Hyper-V 的虚拟机。
  - 几秒钟内启动 Moby。
  - 在 `PATH` 中安装 CLI。
  - 将 Docker 命令代理到 Moby。

**文件系统**

  - 挂载宿主机文件系统以支持 `--volume`。
  - 使用硬编码密码的 Samba 客户端。
  - 支持热重载。

**网络**

  - 对 Node.js 应用程序进行实时调试。